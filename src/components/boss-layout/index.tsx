import { IDoFRenderCharacter } from '@/src/models/dream-of-five.interfaces';
import styles from './index.module.scss';

export default function BossLayout({ characterDef }: { characterDef: IDoFRenderCharacter }) {
    return <div>test</div>
}